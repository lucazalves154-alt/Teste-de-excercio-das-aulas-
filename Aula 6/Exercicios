import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

//Aluno: Lucas Alves Cordeiro 
//Exercicio 1
  import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        
       int numero = 100;

       while (numero <= 200){
           System.out.println(numero +"");
           numero ++;

//Exercicio 2 
 int numero = 1;
        int contpar = 0;
        int contimpar = 0;

        while (numero <= 10) {
            if (numero % 2 == 0) {
                System.out.println(numero + " - par ");
                contpar++;
            } else {
                System.out.println(numero + " - Impar ");
                contimpar++;
            }
            numero++;
//Exercicio 3
  Scanner sc = new Scanner(System.in);


        int valor;
        int max = 1024;
        System.out.println("Digite um valor: ");
        valor = sc.nextInt();


        while (valor  < max) {
            valor = valor * 2;
            System.out.println(valor);
//Exercicio 4 
 Scanner sc = new Scanner(System.in);

        System.out.print("Digite um número entre 10 e 20: ");
        int numero = sc.nextInt();

        if (numero >= 10 && numero <= 20) {
            System.out.println("Metade: " + numero / 2.0);
        } else {
            System.out.println("Número fora do intervalo!");
        }

        sc.close();
//Exercicio 6

    Scanner ns = new Scanner(System.in);

        System.out.println(" Digite 1 nomero : ");
        int menor = ns.nextInt();

        int i = 1;

        while (i <= 10) {
            System.out.println(" Digite outro numero : ");
            int num = ns.nextInt();

            if (num < menor) {
                menor = num;
            }
            i++;
        }
            System.out.print(" Menor numero = " + menor);

    }
}

//Ecercicio 7
 Scanner ns = new Scanner(System.in);

                int contador = 0;
                int i = 1;

                while (i <= 10) { // se 1 for igual ou maior que 2 = verdadeiro
                    System.out.print("Digite sua altura: ");
                    double altura = ns.nextDouble();         // Pergunta altura do usuario

                    System.out.print("Digite seu peso: ");     // Pergunta o peso do usuario
                    double peso = ns.nextDouble();

                    double imc = peso / (altura * altura);   // Com para a altura das duas pessoas

                    if (imc >= 18.5 && imc <= 24.9) {    // fala qual peso esta ideal
                        contador++;    // Incrementa mais 1 
                    }

                    i++; // incrementa mais 1 no resultado
                }

                System.out.println("Quantidade de pessoas no peso ideal: " + contador);  // Mostra peso ideial
            }
        }


 
